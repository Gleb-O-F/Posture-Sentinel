mod camera;
mod inference;
mod posture;
mod config;
mod overlay;

use anyhow::Result;

fn main() -> Result<()> {
    println!("AI Posture Sentinel starting...");
    
    // TODO: Initialize components
    // 1. Load config
    // 2. Initialize camera
    // 3. Load ONNX model
    // 4. Start main loop
    
    Ok(())
}
